let initialState = {
    friends: [
        {name: 'Sveta'},
        {name: 'Sasha'},
        {name: 'Ira'}
    ]
}

const sibebarReducer = (state = initialState, action) => {
    return state;
}

export default sibebarReducer;